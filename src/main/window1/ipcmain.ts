
import { ipcMain, BrowserWindow, dialog} from "electron";

// 监听渲染进程事件
export async function window1ListenIpcMsg(window:BrowserWindow|null) {
    // // 渲染进程传来 打开文件
    // ipcMain.on(IPCMAIN_MARKDOWN_OPENFILE, async (event: any, arg: any) => {
        
    // });
}

  
